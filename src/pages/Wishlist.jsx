import React from 'react';
// TODO: Add flip cards, add-your-own ideas (localStorage)
const Wishlist = () => (
  <div className="wishlist-page">
    <h2>Wishlist / Future Plans</h2>
    {/* Places, things, food, movies, couple goals */}
  </div>
);
export default Wishlist;
